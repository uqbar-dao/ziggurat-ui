export interface RawMetadata {
  name: string
  symbol: string
  decimals?: string
  properties?: string[]
  supply: string
  cap: string
  mintable: string
  minters: string[]
  deployer: string
  salt: string
}
